<UserControl x:Class="EnergonSoftware.Launcher.Controls.FriendsListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:launcherprops="clr-namespace:EnergonSoftware.Launcher.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid HorizontalAlignment="Center" VerticalAlignment="Bottom">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Row="0" Grid.Column="0" Margin="5" HorizontalScrollBarVisibility="Auto" Visibility="{Binding ShowFriendsList, Converter={StaticResource BoolToVis}}">
        </ScrollViewer>

        <Button Grid.Row="1" Grid.Column="0" Margin="5" Width="90" Click="ButtonFriends_Click"><TextBlock Text="{Binding FriendButtonText, FallbackValue='Friends: (9999)'}" /></Button>
    </Grid>
</UserControl>
