<Window x:Class="EnergonSoftware.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:launcherprops="clr-namespace:EnergonSoftware.Launcher.Properties"
        xmlns:launchercontrols="clr-namespace:EnergonSoftware.Launcher.Controls"
        Title="{x:Static launcherprops:Resources.MainWindowTitle}"
        Height="350" Width="525" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    <Grid Margin="5" HorizontalAlignment="Center" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <launchercontrols:LoginPage Grid.Row="0" Grid.Column="0" Margin="5" Visibility="{Binding NotAuthenticated, Converter={StaticResource BoolToVis}}" />
        <launchercontrols:MainPage Grid.Row="1" Grid.Column="0" Margin="5" Visibility="{Binding Authenticated, Converter={StaticResource BoolToVis}}" />
    </Grid>
</Window>
